# Supabase (for Auth). Get these from Supabase Dashboard → Project Settings → API.
# Use the anon/public key in the browser (never the service_role key here).
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_public_key
